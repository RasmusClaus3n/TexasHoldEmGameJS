import { mainPlayer, cpuPlayers, allPlayers } from './game.js';

function logPlayers() {
  console.log(allPlayers);
}

function setBlinds(round, allPlayers) {
  let smallBlind = 1;
  let bigBlind = 5;
}

export { logPlayers };
